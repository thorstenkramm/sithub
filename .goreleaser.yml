version: 2

builds:
  - main: ./cmd/sithub
    binary: sithub
    env:
      - CGO_ENABLED=1
    goos:
      - linux
    goarch:
      - amd64

archives:
  - formats:
      - tar.gz
    files:
      - LICENSE
      - sithub.example.toml

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

release:
  github:
    owner: thorstenkramm
    name: sithub
